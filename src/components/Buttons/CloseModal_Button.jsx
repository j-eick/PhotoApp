import { Styled_CloseModal_Button } from "../../globalStyles";

const CloseModal_Button = ({ onClick }) => {
  return (
    <Styled_CloseModal_Button onClick={onClick}>Close</Styled_CloseModal_Button>
  );
};

export default CloseModal_Button;
